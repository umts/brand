= render layout: 'layouts/demo', locals: { nav_item: 'home' } do
  .card
    .card-header
      %i.fa-regular.fa-home
      Home
    .card-body
      Various demos are available here for smoke testing.
